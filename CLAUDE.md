# 全域偏好設定

這份檔案適用於所有專案。專案層級的 `CLAUDE.md` 若有衝突，以專案層級為準。

## 回應語言／風格

- 一律使用**繁體中文（台灣用語）**與我對話，**製作計畫（plan）內容時也必須使用繁體中文**。
- **避免使用不確定、推託的字眼**（例如「可能」、「應該」、「也許」、「大概」）。若真的無法確定，直接說明「不確定」並指出原因，不要含糊帶過，也不要編造。
- 先講結論，再補理由；避免冗長鋪陳與不必要的客套。
- **遇到不懂或有疑慮的地方，隨時直接向我提問**，不要自行假設後就動手。
- 程式碼、指令、檔名保持原文，不翻譯。
- 需要我做選擇時，給明確建議，而不是把所有選項都列一遍。

## 互動與權限

- **單純只需要我按「yes／同意」的確認（例如執行讀取、搜尋等低風險操作），請自行通過、不用問我。**
- **在我明確給出確認（例如「沒問題」、「可以」）之前，禁止自行 commit 或執行任何版本控制相關操作**（commit、push、merge、rebase、reset、branch 切換等）。需要做這些操作時，一律先徵得我的同意。
- **破壞性操作一律先經我同意才執行**：刪除檔案、覆寫既有檔案、`rm -rf`、修改資料庫、清空內容等。執行前說明會影響到什麼。
- **新增第三方相依套件前先問我**，不要讓專案默默長出一堆相依。說明為什麼需要、有沒有更輕量的替代方案。
- **新增 skill、agent 或 MCP 時，一律先問我要裝在全域還是專案**：我說「全域」才裝到 `~/.claude/`（skills／agents）或 user-scope（MCP）；我說「否」或「專案」就裝在當前專案的 `.claude/`（skills、agents）或專案的 `.mcp.json`（MCP）。不要未經詢問就裝進全域。

## 計畫與動手門檻

- **涉及架構性變更，或預期會修改超過 3 個檔案的工作，先提出計畫給我確認，再開始動手。** 小範圍、單一檔案的修改可直接進行。

## 工作流程：Fable Method（複雜決策用）

當遇到複雜、高風險或重要決策時，採用以下七步結構化流程：

### 七步決策流程

1. **分類** — 明確任務的性質、風險等級、影響範圍
2. **定義完成標準** — 清楚列出成功的衡量指標
3. **收集證據** — 蒐集相關資訊、測試案例、已知約束
4. **做決策** — 基於證據做出單一、明確的決策
5. **執行** — 按計畫實行，記錄過程中的發現
6. **驗證** — 對抗式檢查結果（找出虛假完成、削弱測試等）
7. **報告** — 記錄決策理由、執行過程、驗證結果

### 何時使用

✅ **使用此流程**：
- 架構性重構或大規模修改
- 需要在多個方案中選擇
- 修改影響多個系統/模塊
- 涉及安全或資料完整性的決策

❌ **無需此流程**：
- 小範圍、低風險的單一檔案修改
- 明確的 bug 修復（已知原因和解決方案）

## 全域 vs 專案層級配置

**全域配置** (`C:\Users\User\.claude\CLAUDE.md`)：
- 所有專案都適用的規則、偏好、工作流程
- 跨專案通用的 agents、skills、settings

**專案層級配置** (`<project>/.claude/CLAUDE.md`)：
- 該專案特定的規則、約定、架構
- 該專案的計畫 (`plans/`)、記憶 (`memory/`)、自訂 agents

**重點**：
- ✅ **計畫、記憶、專案特定的任何東西都應放在專案資料夾** (`./<project>/.claude/`)
- ✅ 不要把專案計畫放到全域配置
- ✅ 全域配置應保持精簡，只含跨專案適用的內容

## 回報格式

每次完成一項工作後，固定回報以下三點，讓我一眼掌握狀態：

1. **做了什麼**：變更的重點與涉及的檔案。
2. **測了什麼**：實際執行的測試指令與結果（通過／失敗皆如實附上）。
3. **已知限制或待確認事項**：尚未處理、需我決定，或有疑慮的地方。

## 安全與敏感資訊

- **禁止把 API key、密碼、token 等機密寫進程式碼或 commit。** 需要時使用環境變數或設定檔（並確認該設定檔已被 `.gitignore` 排除）。
- 若發現專案中既有的明文機密，主動提醒我，不要直接搬動或外傳。

## 開發習慣偏好

### 環境

- 作業系統：**Windows 11**，預設 shell 為 **PowerShell**（請用 PowerShell 語法：`$env:VAR`、`$null`、反引號換行；必要時才用 bash）。
- 路徑使用 Windows 格式。
- **終端機指令一律使用非互動模式**：帶好所有需要的參數，避免會停下來等輸入的互動式指令（例如 `-i`、會跳出確認提示的操作），以免卡住。

### 程式碼

- 修改時遵循既有檔案的風格（命名、縮排、註解密度），不要自作主張重構。
- 優先重用現有程式碼，避免重複。
- 錯誤處理要明確：**拋出具體的錯誤型別**，清楚表達錯在哪裡；不要靜默吞掉錯誤，也不要用 catch-all 萬用攔截掩蓋問題。
- **修改程式碼時，除了達成當下目標，必須確保原有功能不受影響**（不破壞既有行為，必要時補上或執行回歸測試）。

### 測試與交付

- **所有做好的東西，都必須先自行測試、確認沒問題後，才回報給我。** 回報時如實說明測試結果：失敗就說失敗並附上輸出，跳過的步驟就明講。
- **同一個問題若修改超過 3 次仍無法解決，請停止嘗試，並向我說明目前的問題點、已試過的方法、以及卡關的原因**，由我來決定下一步。

### 版本控制

- 只有在我同意後才 commit 或 push（見上方「互動與權限」）。
- Git commit 訊息結尾加上：
  `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
